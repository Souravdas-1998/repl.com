# ============================================================
# StyleAI — PRODUCTION environment TEMPLATE
# Fill in real values via your secrets manager (AWS SSM, Vault, etc.)
# Do NOT commit real values — use this as a reference only
# ============================================================

NODE_ENV=production
LOG_LEVEL=warn

PORT=5000
SESSION_SECRET=REPLACE_WITH_STRONG_PRODUCTION_SECRET_MIN_32_CHARS

DATABASE_URL=postgresql://styleai_prod:REPLACE_PASSWORD@prod-db.internal:5432/styleai_prod
PGHOST=prod-db.internal
PGPORT=5432
PGUSER=styleai_prod
PGPASSWORD=REPLACE_PASSWORD
PGDATABASE=styleai_prod

VITE_API_BASE_URL=/api
BASE_PATH=/

# AI Provider (optional — enable if integrating OpenAI/Anthropic)
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...

DOCKER_REGISTRY=REPLACE_REGISTRY
ECS_CLUSTER=styleai-cluster
ECS_SERVICE_API=styleai-api-service
ECS_SERVICE_WEB=styleai-web-service
AWS_REGION=us-east-1
PROD_URL=https://styleai.example.com
